# Group company logos

Drop transparent **.png** logos with these exact filenames (mono / grayscale works best, ~400 px wide):

```
match-hospitality.png
match-services.png
match-accommodation.png
gts-events.png
```

While missing, each card shows the company name instead.
Edit `GROUP` in `src/js/SectionsEnd.jsx` to add or change links.
